# Object-Oriented Programming

- another style of programming
(another way we write our code)

- organizing our code into objects

Procedural Programming:
Procedure => a set of step-by-step instructions
          => a function

Object Oriented Programming (OOP)
= organize our code into objects

let variable; is shortcut for
let variable = undefinded;

Function inside an object => method

'this' gives us outer object

Why do we use OOP?
Object-Oriented Programming = tries to represent the real world

import '../data/cart-oop.js';
another way to import

another reason:
Easy to create multiple objects

In OOP:
Use PascalCase for things that generate objects
PascalCase = start every word with a capital

